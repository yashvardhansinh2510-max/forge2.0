# Local/preview fallback only. Copy to frontend/.env, or run
# `scripts/setup-env` from the repo root. Never commit frontend/.env.

# Base URL the Expo app calls for all API requests — no trailing slash.
EXPO_PUBLIC_BACKEND_URL=http://localhost:8000
