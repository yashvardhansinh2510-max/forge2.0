[pytest]
# The supported automated suite lives here.  The repository root also keeps
# one-off operational probes whose filenames predate the test layout; pytest
# must not import those scripts as tests (some intentionally require a live
# environment or exit during their safety checks).
testpaths = backend/tests/unit
python_files = test_*.py
